<svg width="880" height="292" viewBox="0 0 880 292" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-labelledby="aboutTitle aboutDesc">
  <title id="aboutTitle">About William</title>
  <desc id="aboutDesc">Background and competitive programming highlights</desc>
  <style>

    :root {
        --night: #08090b;
        --surface: #0e1014;
        --raised: #14171c;
        --ink: #e8e6e1;
        --muted: #8b919c;
        --line: #20242b;
        --acid: #c8f750;
        --acid-dim: #8fb52e;
    }

    .font-display {
        font-family: "Syne", ui-sans-serif, system-ui, sans-serif;
        font-weight: 800;
        letter-spacing: -0.03em;
        text-transform: uppercase;
        fill: var(--ink);
    }
    .font-mono {
        font-family: ui-monospace, "JetBrains Mono", SFMono-Regular, Menlo, monospace;
        letter-spacing: 0.2em;
        text-transform: uppercase;
    }
    .font-body {
        font-family: "Bricolage Grotesque", ui-sans-serif, system-ui, sans-serif;
        letter-spacing: 0;
        text-transform: none;
        fill: var(--ink);
    }
    .bg { fill: var(--night); }
    .surface { fill: var(--surface); }
    .ink { fill: var(--ink); }
    .muted { fill: var(--muted); }
    .acid { fill: var(--acid); }
    .line { stroke: var(--line); }

    @keyframes rise {
        from { opacity: 0; transform: translateY(12px); }
        to { opacity: 1; transform: translateY(0); }
    }
    @keyframes fade {
        from { opacity: 0; }
        to { opacity: 1; }
    }
    @keyframes sweep {
        from { transform: scaleX(0); }
        to { transform: scaleX(1); }
    }
    @keyframes drift {
        0%, 100% { transform: translate(0, 0); }
        50% { transform: translate(8px, -6px); }
    }
    @keyframes pulse {
        0%, 100% { opacity: 0.25; }
        50% { opacity: 0.85; }
    }
    @keyframes cellIn {
        from { opacity: 0; transform: scale(0.65); }
        to { opacity: 1; transform: scale(1); }
    }

    .rise { animation: rise 0.7s cubic-bezier(0.22, 1, 0.36, 1) both; }
    .fade { animation: fade 0.8s ease both; }
    .bar-grow {
        transform-box: fill-box;
        transform-origin: left center;
        animation: sweep 0.9s cubic-bezier(0.22, 1, 0.36, 1) both;
    }
    .underline-grow {
        transform-box: fill-box;
        transform-origin: left center;
        animation: sweep 0.85s cubic-bezier(0.22, 1, 0.36, 1) both;
    }
    .drift { animation: drift 8s ease-in-out infinite; }
    .pulse { animation: pulse 3.2s ease-in-out infinite; }
    .cell-in {
        transform-box: fill-box;
        transform-origin: center;
        animation: cellIn 0.45s cubic-bezier(0.22, 1, 0.36, 1) both;
    }

    .d1 { animation-delay: 0.05s; }
    .d2 { animation-delay: 0.12s; }
    .d3 { animation-delay: 0.2s; }
    .d4 { animation-delay: 0.28s; }
    .d5 { animation-delay: 0.36s; }
    .d6 { animation-delay: 0.44s; }
    .d7 { animation-delay: 0.52s; }
    .d8 { animation-delay: 0.6s; }

    @media (prefers-reduced-motion: reduce) {
        .rise, .fade, .bar-grow, .underline-grow, .drift, .pulse, .cell-in {
            animation: none !important;
            opacity: 1 !important;
            transform: none !important;
        }
    }

  </style>

  <defs>
    <linearGradient id="aboutWash" x1="0" y1="0" x2="880" y2="292" gradientUnits="userSpaceOnUse">
      <stop stop-color="var(--acid)" stop-opacity="0.08"/>
      <stop offset="1" stop-color="var(--night)" stop-opacity="0"/>
    </linearGradient>
  </defs>


  <rect width="880" height="292" class="bg"/>
  <rect width="880" height="292" fill="url(#aboutWash)"/>
  <rect x="0.5" y="0.5" width="879" height="291" fill="none" stroke="var(--line)"/>


  <g class="rise d1">
    <rect x="39.5" y="29.5" width="5" height="5" fill="var(--acid)" transform="rotate(45 42 32)"/>
    <text x="56" y="36" class="font-mono" style="font-size:11px; fill: var(--acid)">01 / ABOUT</text>
    <text x="40" y="68" class="font-display" style="font-size:28px">ABOUT</text>
  </g>

  <text x="40" y="104" class="font-body rise d2" style="font-size:16px; fill: var(--ink)">I build compilers, trading systems, AI tooling, and contest</text><text x="40" y="128" class="font-body rise d3" style="font-size:16px; fill: var(--ink)">software - all open source on GitHub.</text>
  
      <g transform="translate(40, 180)">
        <g class="rise d4">
          <rect x="1.5" y="-6.5" width="5" height="5" fill="var(--acid)" transform="rotate(45 4 -4)"/>
          <text x="18" y="0" class="font-body" style="font-size:14px; fill: var(--muted)">ICPC European Championship 2026 - 2nd in France, 40th in Europe</text>
        </g>
      </g>
      <g transform="translate(40, 208)">
        <g class="rise d5">
          <rect x="1.5" y="-6.5" width="5" height="5" fill="var(--acid)" transform="rotate(45 4 -4)"/>
          <text x="18" y="0" class="font-body" style="font-size:14px; fill: var(--muted)">Castor Informatique winner among 500k+ participants</text>
        </g>
      </g>
      <g transform="translate(40, 236)">
        <g class="rise d6">
          <rect x="1.5" y="-6.5" width="5" height="5" fill="var(--acid)" transform="rotate(45 4 -4)"/>
          <text x="18" y="0" class="font-body" style="font-size:14px; fill: var(--muted)">4x Prologin finalist (2022-2025)</text>
        </g>
      </g>
</svg>
